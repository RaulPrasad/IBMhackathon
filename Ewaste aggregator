The Ewaste Aggregator App 
The app shows a list of available E-waste handlers in the users vicinity and the user can select a slot for the vendor to collect the waste from the user.
The user gets monetary reward along with social media badge of recognition for each e-waste transaction.
